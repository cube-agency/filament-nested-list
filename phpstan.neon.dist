# larastan and the phpstan-* rule sets are wired up automatically by
# phpstan/extension-installer, so they must not be included here as well.
parameters:
    level: 8
    paths:
        - src
    tmpDir: build/phpstan
